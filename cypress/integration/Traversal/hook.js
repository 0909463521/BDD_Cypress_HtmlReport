beforeEach(() => {
    const state = window.testState
    cy.log(
        window.Cypress
    )

    cy.visit('https://example.cypress.io/commands/traversal');
});
before(function () {
    cy.log('Before all Traversal Page');
});



  